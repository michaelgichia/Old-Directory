import expect from 'expect';
// import { selectDirectoryLandingPageDomain } from '../selectors';

// const selector = makeSelectDirectoryLandingPageDomain();

describe('makeSelectDirectoryLandingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

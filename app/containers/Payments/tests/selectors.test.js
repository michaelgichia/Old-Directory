// import { fromJS } from 'immutable';
// import { makeSelectPaymentsDomain } from '../selectors';

// const selector = makeSelectPaymentsDomain();

describe('makeSelectPaymentsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
